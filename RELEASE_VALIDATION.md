# v0.7.0 release validation

## Scope

能量反卷积 (多宽度扫描 + 曲率正则增广最小二乘)、传播收敛检查、
CLI/dashboard 接入, 以及配套教材 (book/)。v0.4.0-v0.6.0 的验证结论
继续有效。

## 反卷积: 病态性处理与实测

**实现教训 (已修复)**: 正规方程 (AᵀA) 会平方条件数。cond(A) ~1e9
时 AᵀA 条件数 ~1e18, 触及 float64 噪声底, 解在 null 空间方向发散
成 0/1 振荡。正确解法: 对增广系统 [A; √λ·D₂] 直接做 SVD 最小二乘
(lstsq), 曲率正则 D₂ᵀD₂ 对 T(E) 台阶形状比 L2 幅值正则自然。

**理想数据验证**: 解析 logistic 阈值经三宽度包络卷积后, 反卷积
恢复 RMSD < 0.005; 2% 噪声下 RMSD 0.003。

**真实传播数据 (可分离 Eckart, T(E) 已知)**:

| 设置 | cond(A) | 残差 | 与 1D 参考的平均差 |
|---|---|---|---|
| 宽窗 0.005-0.045, 6000 步 (传播充分) | 1.3e3 | 0.019 | ~0.11 (误差集中于阈值跳变) |
| 窄窗 0.025-0.045, 4000 步 | 1.9e9 | 0.38 | 0.47 (无意义, 被诊断标记) |

**两个诊断量各司其职** (测试固定): 条件数刻画病态性 (窄窗 >
1e6); 相对残差刻画数据与包络模型的失配 (交替数据 > 0.3; 一致的
常数数据即使在病态窄窗下残差也可低)。

**engine CLI 冒烟** (1200 步, 2 扫描点): 收敛检查报告
P_react 0.548 → 0.295 (Δ=0.25, 告警未收敛); 反卷积残差 0.17
(告警)。两个工具按设计暴露数据质量问题, 不静默输出。

## 收敛检查

`check_convergence` 在可分离 Eckart (128×64, dt=0.5, 3000 步,
E=0.04) 上: 基线 vs 加密设置 |ΔP_react| < 0.03 (回归测试断言)。

## 测试与 wheel 验证

- 全量 41 项测试通过 (macOS arm64, Python 3.14.5), 87 s。
- 全新 venv 安装 `autoquantum-0.7.0-py3-none-any.whl`: 版本元数据
  0.7.0; 41 项测试通过; CLI --version/info 正常; pip check 干净。

## 教材 (book/)

- 10 章正文 (数学预备 → 量子力学 → 势能面 → NN → 定态/含时 →
  二维动力学 → 验证 → 架构 → 进阶) + 前言与学习路径 + 附录 A
  (85 条术语/公式速查) + 附录 B (14 个真实陷阱 + 15 道分级练习),
  约 134 KB; 每章含"新手路径/高手专栏"双层结构; 全部 API 引用
  由撰写者对照源码逐条核验; 实验性/未实现部分均有明确标注。

## 不在本版验证/修复范围内

- 反卷积在真实数据上的点值精度受病态性限制 (本版给出诊断与诚实的
  误差量级, 不声称高精度恢复);
- 从头算梯度 (ASE/PySCF); 2D 定态求解器仍为实验性;
- LEPS 教学参数阈值非真实物理量; 跨平台/多 Python 版本矩阵。
